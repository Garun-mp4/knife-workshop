"use client";
import { useEffect, useState } from "react";
import { apiGet, apiSend } from "../../lib/api";
export default function Reviews(){ const [items,setItems]=useState<any[]>([]); const load=()=>apiGet<any[]>('/admin/reviews').then(setItems); useEffect(()=>{load()},[]); async function create(fd:FormData){ const p:any=Object.fromEntries(fd.entries()); p.rating=p.rating?Number(p.rating):undefined; p.isPublished=p.isPublished==='on'; await apiSend('/admin/reviews','POST',p); load(); } return <div><h1>Отзывы</h1><form action={create} className="card" style={{padding:20, display:'grid', gap:12}}><input className="input" name="clientName" placeholder="Клиент" required/><input className="input" name="city" placeholder="Город"/><textarea className="input" name="text" placeholder="Текст" required/><input className="input" name="rating" placeholder="Рейтинг 1-5"/><label><input type="checkbox" name="isPublished"/> Опубликован</label><button className="btn">Добавить</button></form><div className="card" style={{marginTop:20}}><table className="table"><tbody>{items.map(r=><tr key={r.id}><td>{r.clientName}</td><td>{r.city}</td><td>{r.rating}</td><td>{r.isPublished?'да':'нет'}</td></tr>)}</tbody></table></div></div> }
