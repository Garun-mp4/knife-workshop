"use client";
import { useEffect, useState } from "react";
import { apiGet, apiSend } from "../../lib/api";
export default function Pages(){ const [items,setItems]=useState<any[]>([]); const load=()=>apiGet<any[]>('/admin/pages').then(setItems); useEffect(()=>{load()},[]); async function create(fd:FormData){ await apiSend('/admin/pages','POST',Object.fromEntries(fd.entries())); load(); } return <div><h1>Страницы</h1><form action={create} className="card" style={{padding:20, display:'grid', gap:12}}><input className="input" name="title" placeholder="Title" required/><input className="input" name="slug" placeholder="slug" required/><textarea className="input" name="content" placeholder="Content" required/><select className="input" name="status"><option>PUBLISHED</option><option>DRAFT</option><option>HIDDEN</option></select><button className="btn">Создать</button></form><div className="card" style={{marginTop:20}}><table className="table"><tbody>{items.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.slug}</td><td>{p.status}</td></tr>)}</tbody></table></div></div> }
